# tenancy

This library implements schema-based multi-tenancy with typeform using `search_path` for tenant isolation.

## Running unit tests

Run `nx test tenancy` to execute the unit tests via [Jest](https://jestjs.io).
